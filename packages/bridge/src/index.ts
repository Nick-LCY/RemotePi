// M1 scaffold — placeholder entry. M2 will spawn pi child processes, parse
// JSONL, and own the SessionManager.
import { PROTOCOL_VERSION } from '@remotepi/shared';

console.log(`[bridge] RemotePi bridge v0.0.0 (protocol v${PROTOCOL_VERSION}) — ready`);
