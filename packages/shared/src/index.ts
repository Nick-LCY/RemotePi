// Placeholder exports — M1 scaffold. The envelope / message schemas live in
// ./protocol/* and are filled in by task 02-shared-envelope-prototype.
export const PROTOCOL_VERSION = 1 as const;
export type ProtocolVersion = typeof PROTOCOL_VERSION;
