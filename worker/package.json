{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "description": "RemotePi Cloudflare Worker (M1: hello handler).",
  "scripts": {
    "dev": "wrangler dev",
    "dev:inspector": "wrangler dev --inspector-port=9229",
    "deploy:cf": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir=dist",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@remotepi/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "vitest": "^2.1.9",
    "wrangler": "^4"
  }
}
