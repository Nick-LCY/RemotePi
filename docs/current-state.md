# 当前状态

> 高频变更的工作看板。**开始任何任务前先读这里。** 由团队维护。保持轻量——它是看板，不是叙事。

## 活跃需求
- [[roadmap.md]] — 路线图 M1–M4 已定稿（**2026-09-04 用户定稿**：基建 → 通路 → 单 session 闭环 → 多 session 管理）。下一步按里程碑逐个出 [[prds/README.md|PRD]]。
- [[prds/m1-infrastructure.md|M1 PRD]] 已定稿（基础设施基座，2026-09-04 落盘）；任务已拆至 [[tasks/m1/01-monorepo-scaffold.md|tasks/m1/01]] ~ [[tasks/m1/05-github-actions-ci.md|05]]，已全部 done（5/5 任务 + CI 双绿 + 真实环境闭环，2026-09-05 收官），详见 [[tasks/README.md|任务索引]]。
- [[prds/m2-tunnel.md|M2 PRD]] 已定稿落盘（2026-09-05，基于协议 v1）；任务已拆至 [[tasks/m2/01-shared-envelope-v1.md|tasks/m2/01]] ~ [[tasks/m2/06-deploy-and-validation.md|06]]（6 个，03/04/05 可并行），01–05 done，06 doing（部署形态已改定：主域 + Worker Static Assets + CD 上 Actions；详见 PRD §方案修订注记）。交付约定：本地 commit 不 push，push 由用户验证后手动触发 CD（actions/deploy.yml）。 **M2 ✅ 完成（2026-09-05 线上验收）**
- [[prds/m3-single-session.md|M3 PRD]] 已定稿落盘（2026-09-05，单 session 闭环：bridge 接 pi 子进程 + web 聊天界面 + 4 类阻塞弹窗 + 状态恢复；基于协议 v1 破锁新增 control `get_state` 9 type、envelope (a) 演进加 `session_state.payload.blocked_on` 与 `extension_ui_response.web.value: string|boolean`）。任务已拆至 [[tasks/m3/01-shared-protocol-v2.md|tasks/m3/01]] ~ [[tasks/m3/08-docs-and-validation.md|08]]（8 个；01 根，02 依赖 01；03 依赖 01；04 依赖 01+03；05 依赖 01+03+04；06 依赖 01；07 依赖 01+06；08 收尾依赖 03+04+05+06+07；详见 PRD §任务拆分）。**2026-09-08 增补任务 09-13**（去隔离改造 + ADR-0007 + 集成测试基建 + ADR-0008 + E2E 三任务 + ADR-0009），11/12/13 已 done（5 笔本地 commit 落地，见 [[#任务看板]] + [[#最近变更]] 2026-09-08 E2E 落地条）。交付约定沿用 M2：本地 commit 不 push。
- [[architecture/protocol/README.md|architecture/protocol/]] —— RemotePi 隧道协议唯一真相源，**v1 已定稿**（2026-09-05）：信封 {v, kind(control|pi), type, id, session?, reply_to?, payload}；control 8 个 type（handshake/ping/pong/bridge_status/session_state/session_list/result/error）、pi 9 个 type（prompt/steer/follow_up/abort/get_messages/extension_ui_response/command_result/snapshot/event）；中间层只深度处理 handshake（鉴权）、bridge_status 与 error（自己生成），其余一律转发。

## 任务看板
| Task | 状态 | 备注 |
|------|------|------|
| [[tasks/m1/01-monorepo-scaffold.md\|01 脚手架]] | done | 脚手架+四包空壳+.vscode 四件套；review 通过（修复 prettierignore/shared 双脚本/launch.json 字段/wrangler→4.128+compat 2025-09-01 等 7 项）|
| [[tasks/m1/02-shared-envelope-prototype.md\|02 信封雏形]] | done | Zod envelope + 5 用例全绿；review 通过，修复冗余再导出并裁定命名契约 |
| [[tasks/m1/03-hello-worker.md\|03 hello worker]] | done | 返回文案对齐 + wrangler dev curl 实测 + getting-started/README 落地 |
| [[tasks/m1/04-terraform-cloudflare.md\|04 Terraform CF]] | done | 代码交付 + 无凭据验证全绿；5.x 资源名适配 + 防御性 account filter；用户已本地 apply 成功，remote-pi.sankabox.com 实测返回 hello |
| [[tasks/m1/05-github-actions-ci.md\|05 CI 骨架]] | done | 两 workflow 首跑 terraform 绿/ci 红于双重版本指定，修复后 aa87770 双绿（CI + Terraform）|
| [[tasks/m2/01-shared-envelope-v1.md\|01 shared envelope v1]] | done | shared 协议 v1 重写完成；review 通过（无 Critical），修复 changed_at 毫秒精度显式契约等 5 项（d83ba61）|
| [[tasks/m2/02-shared-tests-rewrite.md\|02 shared tests rewrite]] | done | 22 条测试全绿（17 条 PRD 对应 + 精度钉桩等 5 条）；与 01 同线完成 |
| [[tasks/m2/03-bridge-client.md\|03 bridge client]] | done | bridge 客户端 + 8 单测；review 修复 nonce 升 full UUID / pong 严格配对 / 判死 close 1000（6d58727）|
| [[tasks/m2/04-worker-do-room.md\|04 worker+DO]] | done | worker+Room DO / 路由鉴权 / 转发广播 / 探活判死 / 错误码矩阵；workers-types ^5；wrangler dry-run 通过（b912a5a）|
| [[tasks/m2/05-web-components.md\|05 web 组件]] | done | web 四组件 + WsClient；token 仅内存+hash；build 过（1c252d8）|
| [[tasks/m2/06-deploy-and-validation.md\|06 deploy+验证]] | done | CD 首跑成功（route 切换 + 网页上主域）+ 用户线上验收通过；remotepi-hello 已由用户经 Dashboard 删除（dfbfe7b → 896214a 部署链） |
| [[tasks/m3/01-shared-protocol-v2.md\|01 shared protocol v2]] | done | pi 9 schema 真化 / control 9 type 破锁 / session_state blocked_on / web wire refine / 协议四文档+ADR-0006 同步；literals+envelope-base 拆分；6568b4b，review 通过 |
| [[tasks/m3/02-shared-tests.md\|02 shared tests]] | done | 4 文件 61 条新增（超 25 下限）/ confirm boolean 双用例独立 / 两处 schema 行为偏差已挂账（block-on case 11 timeout 字段 / get-state case 9 ok:false 缺 error）/ 102 全绿 / 9bc5bad |
| [[tasks/m3/03-bridge-config-file.md\|03 bridge config]] | done | config.ts zod strict + work_dir 三连校验 / CLI+env+peerDep 删尽 / 未知 flag 静默 / logger.error 切 stderr / 41 测试全绿 + tsx 冒烟 / 3fbd93e |
| [[tasks/m3/04-bridge-pi-process.md\|04 bridge pi process]] | done | 5 相位状态机 + 自主 kill 标记 + exit 三路径 + session 扫描 + outstanding 命令表；**review 捕获两条 wire 硬伤修复**：`get_messages` 回执走 `snapshot` envelope + `reply_to` 回显 web 命令 id（outstanding 表反查；原实现 command_result+随机 UUID 会破坏任务 07 双查询恢复仪式）；ready 阶段忽略 `agent_settled` 裁定（PRD §2.3 只定义 running→idle）/ auth.json 提示 stderr / extension_ui_response 桩留 TODO(task 05) / cwd 真实比对留 TODO(task 08)；bridge **41→87 测试**；commit `75fad56` |
| [[tasks/m3/05-bridge-popup-core.md\|05 bridge popup]] | done | `extension-ui.ts` 独立 `ExtensionUIRouter` 类（pending+timeouts Map / 4 类入列 / 5 fire-and-forget 消化 / timeout 镜像 + 5 处竞态原子检查 / wire 翻译三态含 confirm value:false→`confirmed:false` + cancelled:true 不附 value / 多 web 端先答者胜 + 后续答者收 `request_expired` / `clearAll` 防 ghost dialog / S1 duplicate id 告警 / S2 翻译 null 补 `invalid_response` 回执）+ `pi-process.ts` 接入（任务 04 桩替换 `buildExtensionUIRouter` 5 回调 + 广播原则落地）+ `client.ts` 6 命令转发 + control get_state 内存回答；bridge **106 → 154 测试**（新增 47 条）；reviewer 通过零 C 零 W，5S 全修；commit `a466188` |
| [[tasks/m3/06-web-chat.md\|06 web chat]] | done | ChatView（PhaseIndicator/MessageList/QueueIndicator/InputBar）+ 4 类弹窗（ConfirmDialog **三态按钮**：Cancel=cancelled:true / No=value:false / Yes=value:true）+ DialogHost 状态机（open/submitting/expired + 关闭规则）+ 无乐观 UI + §4.5 命令失败 UX（dialog toast + 输入框错误提示） + M2 验证组件（BroadcastLog/PingTester）退役 + commit `073f4c6`；**两条挂账**：①work_dir 降级显示（v1 wire 不携带 bridge 配置，PRD §4.3 实际仅显示 phase）；②ConfirmDialog 三按钮 vs PRD §4.2 表两按钮的语义分离（cancelled 与 value 正交）——均由任务 08 同步 ADR-0003/0004 与 pi.md 时注明 |
| [[tasks/m3/07-web-recovery.md\|07 web recovery]] | done | 双查询仪式（recovery.ts：resolver 注册先于 send、per-reply 5s timer、stale 标志、三态 error、retry/dispose）+ WsClient registerReplyResolver 一次性分流 + result 集中处理同步 store + App 三态渲染（RecoveryInFlight / ChatView / RecoveryErrorCard 重试）+ F5 同路径；review 需修复后通过，**5 Warning 全修**（token 切换重发仪式 / replyResolvers JSDoc / setBlockedOn 浅相等 / 4 处静默吞异常补 warn / tryDecodeGetStateData 共享解码）+ 4 Suggestion 落地（useRef/useCallback / ConnState 类型 / CSS selector 合并 / autoStartConsumedRef 改名）；web build 232KB / 237 测试不变；commit `cbeea2e` |
| [[tasks/m3/08-docs-and-validation.md\|08 docs+validation]] | doing | 文档部分完成：ADR-0003/0004 补注 + getting-started §3.5 配置 JSON 字段说明 + current-state 3 处更新 + grep 验证旧 flag 零残留 + 测试规划落盘（用户裁定 2026-09-07 暂不实施、先落文档；2026-09-08 拆解：假 LLM + 隔离 pi 集成测试部分承接至 [[architecture/decisions/0008-fake-llm-isolated-pi-integration-tests.md\|ADR-0008]]，无头浏览器 E2E 已立项 [[architecture/decisions/0009-headless-browser-e2e.md\|ADR-0009]]（已接受·待实施））；**手测进行中——已验证**：对话闭环（含流式渲染与多轮）、F5 恢复、idle kill 与 session 恢复（cwd 编码修复后）；**待用户验证**：abort、4 类弹窗 wire 层已自动化（见 ADR-0008 §4，bridge `04-extension-dialogs` 6 条 it 钉死 4 类入列 + cancel 5 类消化），web UI 层仍手测、timeout 自答、多 web 端、bridge 重启自愈、线上部署验证；三端联调手测 14 条**不勾选**任务文件清单（用户未逐条确认） |
| [[tasks/m3/09-host-shared-agent-dir.md\|09 去隔离改造]] | done | bridge 复用宿主机 pi agent 目录（用户裁定 2026-09-05）；commit `1985fbd` + ADR-0007 落盘 + docs 链同步（getting-started §3.5 简化 / ADR-0003 补注 / PRD 修订注记 / current-state）|
| [[tasks/m3/10-integration-test-infra.md\|10 integration test infra]] | done | 假 LLM server + 隔离 pi + 真 pi 集成测试基建（[[architecture/decisions/0008-fake-llm-isolated-pi-integration-tests.md\|ADR-0008]] §1-§5 落地，原 docs/testing.md 2026-09-08 拆解至本 ADR）：顶层 `tests/integration/`（6 测试文件 30 条 / 5 helper / 1 fixture 扩展 / 4 fixture agent-dir 文件）+ 根 `package.json` 加 `test:integration` + `typecheck:integration` + .gitignore 加 2 行；commits `3c6656d` + `bb6293b`（review 6W 全修 / 2S 合理跳过）；30 条全绿 ≈11s；CI 四步 workflow 零改动（无 pi 二进制有意跳过）；`packages/*` / `worker/` / `.github/` / 根 `tsconfig` 零改动（既有 PiProcessManager seam 直接消费）|
| [[tasks/m3/11-web-testid-hooks.md\|11 web testid hooks]] | done | web 组件断言钩子（ADR-0009 §决策 4 前置改动）：8 文件 / 22 处 `data-testid` 新增（外加 2 处 `data-count` 计数透出），11 组件全覆盖（RecoveryInFlight/RecoveryErrorCard/MessageList/QueueIndicator/InputBar/DialogHost/ConfirmDialog/SelectDialog/InputDialog/EditorDialog/StatusBar/TokenPrompt）；className/DOM/文案/样式/状态机/事件流零变化；commit `6d470fe`；web build 234.36 KB（+2.36 KB）；review 零 C 零 W 零 S（`git diff` 机械复核通过） |
| [[tasks/m3/12-e2e-harness.md\|12 e2e harness]] | done | `tests/e2e/` 顶层骨架 + 四进程装配 + 场景 (a)（ADR-0009 §决策 1/2/5/6/7 + §开放点 3 落地）：`playwright.config.ts`（独立 runner，`.ts` 通过 ESLint projectService）+ 5 helper（global-setup/teardown/bridge-process/wrangler-process/fake-llm-process + 独立子进程 standalone + env-builder re-export）+ 场景 (a) spec；buildHermeticEnv 抽取至 integration 真源（30 条回归全绿）；敲定点 1-4 全部落定（`.ts` 通过 / 3 条 gitignore / globalSetup + bundle WSS URL 审计 / bridge stdout 探针）；装配顺序 wrangler→bridge 规避 bridge 僵尸；review 合并 5W+3S 修复轮，10 项清理 `bb34aaa`；commit `127595c` + `bb34aaa`（顺带） |
| [[tasks/m3/13-e2e-scenarios.md\|13 e2e scenarios]] | done | 场景 (b) F5 恢复 + (c) 多端弹窗先答者胜（ADR-0009 §决策 3 后半部 + §开放点 3 敲定点 5）：`02-reload-recovery.spec.ts`（逐字段对账 reload 前后 message-row 文本与条数）+ `03-multi-tab-first-responder.spec.ts`（双 context 同 token + A 后答 / B 端观察 + observation annotation 记录 `request_expired`）；假 LLM admin 端点 `POST /__e2e/script` + `GET /__e2e/requests`（精确路径匹配 / loopback-only）；bridge 日志改 `appendFileSync` 跨 run 累积；`flushEachEvent: true` 支撑场景 (a) 多 delta 严格单调断言；review 通过 1W+2S 落地 + 收尾 W5 残留竞态修复；3 场景全绿 ≈10s 连跑 2 次稳定；commit `5b27300` + `bb34aaa` |

依赖链：M1、M2 全 done。M3：01 根任务；02 依赖 01；03 依赖 01；04 依赖 01+03；05 依赖 01+03+04；06 依赖 01；07 依赖 01+06；08 收尾依赖 03+04+05+06+07（06/07 与 03/04/05 两条链可并行）；**10 收尾依赖 04/05**（验证既有 PiProcessManager seam + ExtensionUIRouter 端到端；不阻塞他人，已 done）；**11 → 12/13**（testid 抓手先行；ADR-0009 §开放点 2 建议）；**12 → 13**（场景 (a) 装配 + retry 容错 helper 复用）；**12/13 收尾依赖 11**（testid 锚点消费对象）。M3 任务 11/12/13 已全部 done（2026-09-08 五笔本地 commit 落地）。

## TODO / 阻塞
- [ ]（暂缓，用户裁定）可选加固：remotepi-worker 关闭 workers.dev 入口（workers_dev=false）；清理本地残留 /usr/local/bin/remote-pi-bridge 旧二进制
- [ ] 修复 pnpm dev 启动方式下 bridge 偶发僵尸问题（tsx watch 对脚本崩溃不重启、栈被吞；候选方案：换 node --watch / 外层监督重启 / 根因排查顶层异常；本地联调暂用三终端或 --worker-url 单起绕过）
- [ ]（暂缓，用户裁定 2026-09-05）bridge 配置迁移后将来重新加回 CLI 参数与环境变量支持（M3 仅保留 --config）
- [ ] M4 候选：共享模式下"会话被外部进程占用"检测（同会话双写防护，承载去隔离后果）——见 [[architecture/decisions/0007-host-shared-pi-agent-dir.md|ADR-0007]]
- [ ]（待讨论，2026-09-07）web 恢复仪式 snapshot 5s 超时对 pi 冷启动（exited → spawn → ready → get_messages）不足，实测首启可超 5s 致 snapshot_failed。候选方案：A 拉长超时至 15-30s / B bridge_status 感知（offline 秒失败 + online 给宽限）/ C 相位感知计时器（session_state{phase:'spawning'} 广播重置 snapshot 计时器，ready 后仍未到才判失败，顺带把相位透出 UI）/ D 超时自动重发 get_messages。倾向 B+C 轻量合体。探针数据：pi 冷启动约 3s + get_messages 往返，修完握手后总时长逼近 5s 红线。**e2e 已按 retry 容错运行**（任务 11/12/13 三场景落地，2026-09-08）——本机实测 pi 冷启动 ~500ms，5s 超时未踩中；e2e retry 容错断言链就位（详见 ADR-0009 §开放点 1 短期路径），挂账修复后可简化断言为「纯等 ChatView」；挂账仍开放。
- [ ] 无头浏览器 E2E——**已实施（MVP 三场景落地，2026-09-08）**——见 [[architecture/decisions/0009-headless-browser-e2e.md|ADR-0009]] §实施注记（2026-09-08）+ 任务 [[tasks/m3/11-web-testid-hooks.md|11]] / [[tasks/m3/12-e2e-harness.md|12]] / [[tasks/m3/13-e2e-scenarios.md|13]]；可选后续未做：(d) abort 路径 / (e) idle kill 与 bridge 重启自愈（缩短 `idleTimeoutMs` 加速） / (f) PRD §4.5 命令失败 UX / (g) 4 类弹窗逐一交互——按 PRD §6.5 路线图挂账，本机专用 `pnpm test:e2e` / CI 跳过口径与 ADR-0009 §决策 6 一致。

## 最近变更
- 2026-09-08（无头浏览器 E2E MVP 三场景落地 — [[tasks/m3/11-web-testid-hooks.md|tasks/11]] / [[tasks/m3/12-e2e-harness.md|12]] / [[tasks/m3/13-e2e-scenarios.md|13]] 全部 done，**5 笔本地 commit 未 push**：`96477eb` 任务 11 web testid + `127595c` 任务 12 harness + `1558309` 三任务合并 review 修复轮（W1-W5 + S1/S3/S5）+ `5b27300` 任务 13 场景 b/c + `bb34aaa` 收尾修复轮（W5 残留竞态 + 10 项清理））— 5 commit 归为三任务线 + 两轮 review 修复，[[architecture/decisions/0009-headless-browser-e2e.md|ADR-0009]] 从“待实施”升级为“已落地”，见 ADR-0009 末尾「实施注记（2026-09-08）」：**验收终态**——e2e 3 场景全绿（≈10-11s 连跑 2 次稳定），基线 281 单测 + 30 集成不变，typecheck/lint/build 全绿，CI 四步零改动，web 行为面仅任务 11 的 testid 属性（22 处新增 + 2 处 data-count）；`packages/*` / `worker/` / `.github/` 零改动（任务 10 review 期间漏配的 `tests/integration/.tmp/**` eslint ignore 由任务 12 顺带补配）。**关键实测结论**：①本机 dialog 生命周期仅 36-48ms（远低于 Playwright 默认轮询）——「B 端迟交触发 `request_expired`」的竞态窗口本机不存在，敲定点 5 首选 A 路径降级为「先答者胜 + 双端收起」主断言 + `request_expired` 降为 observation annotation（ADR-0009 §开放点 1 语义在慢速环境/真机仍可能成立）；②pi 冷启动实测 ~500ms（远低于 5s 恢复超时），retry 容错断言未实际触发——但保留（不同机器/首次 SQLite migration 时仍可能踩）；③装配顺序铁律：wrangler 必须先于 bridge 启动——Node 22 WS 对 ECONNREFUSED 只 fire `onerror` 不 fire `onclose`，bridge `handleClose` 不调度重连 → 进程干净退出（规避 bridge 僵尸挂账的装配侧手段，产品代码未动）；④假 LLM 必须独立子进程（Playwright globalSetup worker 退出会带走 in-process server）；⑤本机调试前需清理残留进程（wrangler/workerd/tsx bridge），CI fresh runner 无此问题。详见 [[tasks/m3/11-web-testid-hooks.md|11]] / [[tasks/m3/12-e2e-harness.md|12]] / [[tasks/m3/13-e2e-scenarios.md|13]] + [[architecture/decisions/0009-headless-browser-e2e.md|ADR-0009]]；本条作摘要汇总，**不**逐条展开。
- 2026-09-08（无头浏览器 E2E 立项 — [[architecture/decisions/0009-headless-browser-e2e.md|ADR-0009]] 已接受·待实施，与 ADR-0008 同日成对） — 浏览器 UI 层回归套件从"待另立 ADR"挂账（见 [[architecture/decisions/0008-fake-llm-isolated-pi-integration-tests.md|ADR-0008]] 4 处占位）升级为已落盘设计：Playwright + chromium 驱动 wrangler dev + bridge 子进程 + 真 pi + 进程内假 LLM 四进程全栈链路作为 SUT，MVP 三场景（流式渲染 / F5 恢复 / 多端弹窗先答者胜），本机专用 `pnpm test:e2e` / CI 跳过；与 ADR-0008（bridge wire 层）成对合起来覆盖 bridge→pi→LLM→web 全链路，"手测才能发现"面收窄到极小。**实施任务待排期**（web 组件补 ~15 处 `data-testid` + `tests/e2e/` 套件，详见 ADR-0009 §开放点 2）；建议 (i) `data-testid` 改动零风险先行——让 (ii) 实施者直接有抓手。**实施前建议先决议挂账**：web 恢复仪式 snapshot 5s 超时对 pi 冷启动不足（见 [[current-state.md|current-state TODO]] 2026-09-07 条目，开放点 1），E2E 场景 (a)(b) 首步即踩，建议先落 B+C 轻量合体（bridge_status 感知 + 相位感知计时器）。详见 [[architecture/decisions/0009-headless-browser-e2e.md|ADR-0009]]；本条作摘要汇总，**不**逐条展开。
- 2026-09-08（M3 集成测试基建落地 — [[tasks/m3/10-integration-test-infra.md|tasks/10]] done / 两笔本地 commit `3c6656d` + `bb6293b`） — 假 LLM server + 隔离 pi 集成测试方案从可研落成回归套件，**承接至 [[architecture/decisions/0008-fake-llm-isolated-pi-integration-tests.md\|ADR-0008]]**（同日用户裁定 docs/testing.md 不再留根目录，本 ADR 即其 §2 + §4 + 实施期 review/关键 wire 发现的承接位；§3 无头浏览器 E2E 另立 ADR 挂账于下方 TODO）：**顶层 `tests/integration/`**（6 测试文件 30 条全绿 ≈11s：`00-smoke` 1 + `01-happy-path` 6 + `02-session-persistence` 4 + `03-error-normalization` 3 + `04-extension-dialogs` 6 + `06-multi-turn` 10）+ **5 helper**（`fake-llm-server` 进程内 http 绑 port 0 + `make-fixture` 隔离目录 + `make-manager` 预检 pi 二进制 + `wait-for` + `assertions`）+ **1 fixture 扩展** `test-ext.ts`（`pi.registerTool` 一个 `trigger_dialog` 工具覆盖 5 类 `ctx.ui.*`）+ **fixture agent-dir 四件套**（`models.json` + `settings.json` + `auth.json` + `extensions/test-ext.ts`）；**根 `package.json`** 加 `test:integration` + `typecheck:integration` 两脚本 + **`.gitignore`** 加 `tests/integration/.tmp/` + `tests/integration/**/sessions/`。`packages/*` / `worker/` / `.github/` / 根 `tsconfig` / eslint **零改动**（既有 PiProcessManager seam 直接消费，印证"seam 已就绪"设计）；CI 四步 workflow 不变、有意跳过本套件（无 pi 二进制）。**review**零 Critical / **6 Warning 全修**（最关键 W5：steer 用例补端到端断言——假 server 第二轮回显 + `fakeServer.requests` 落线断言，真正钉死 bridge→pi→LLM 全链路携带 steer 文本，**防 `44960b9` 同类翻译层回归**）/ **2 Suggestion 合理跳过**（S5 `.gitignore` 冗余行按"宁多勿漏"保留 / S7 `cleanup` SIGKILL 兜底——bridge `stop()` 已内建 SIGTERM→1s→SIGKILL，重复无收益）。**验证全绿**：基线 281 单测不变 + 4 包 typecheck + `typecheck:integration` + lint + build。**关键 wire 发现**（pi 0.85.1 实证，已落 ADR-0008 "关键 wire 发现"段）：① LLM 侧失败走 `message_end{stopReason:'error'}` 事件流，prompt 的 `command_result` 在 preflight 恒 success:true（rpc-mode.js:298-323）；② `--session` 恢复后 pi 对 prompt 的处理不确定 → 测试钉 bridge 契约（spawnCount→2）；③ follow_up 需 running 中入队才会被消化，idle 后只入队不触发；④ steer 端到端断言 = 假 server 第二轮回显 + requests 落线双重验证（防 `44960b9` 同类翻译层回归）。**任务 08 注记**：脚本化触发基建已就绪（bridge wire 层，ADR-0008 §4 钉死 4 类弹窗 6 条 it），web UI 层 14 条手测清单仍待用户逐条验收。详见 [[tasks/m3/10-integration-test-infra.md|tasks/10]] + [[architecture/decisions/0008-fake-llm-isolated-pi-integration-tests.md|ADR-0008]]；本条作摘要汇总，**不**逐条展开（ADR / task 文件已逐条记录）。
- 2026-09-07（端到端测试修复轮收口 — **10 笔修复 commit / 12 个问题 / hotfix 时跳 archivist**，本轮文档补账） — M3 联调手测共发现 12 个问题，10 笔修复 commit，其中 5 笔修复 + 2 笔文档 commit 已在前期流转（[[#最近变更]] 2026-09-07 spawning 修复 / cwd 编码 / bridge→pi 翻译层三条），**本条补账剩余 7 笔未流转修复**——hotfix 时 archivist 跳过、本轮统一收口。**按四类归组**列出全部 12 问题 ↔ 10 commit：
  - **wire/落盘细节 vs pi 实物（6 问题 / 6 commit）**：① **pi 失败响应 `error` 形状归一化** — `837d1de` `string→{code,message}` 六分支归一（pi 0.85.1 RPC 恒为 string）；② **bridge→pi 命令帧翻译层** — `44960b9` `DeferredCommand` 持有 web wire，写 stdin 时单点翻译 `content→message`（旧测试反向断言 `not.toHaveProperty('content')` 同步翻转）；③ **pi 裸事件对象分发重写** — `3049567` 旧路径只把 `{type:'event', event:...}` 当 turn-end，全事件曾被静默丢弃，新实现按 pi 真裸形状（`message_update`/`message_end`/`agent_settled`/`extension_ui_request`/`queue_update`/`agent_start`/`agent_end`/`turn_*`）逐 case 分发；④ **web 事件提取器对齐真实 wire + stableKey 位置键** — `c6d2f90` web 端按 pi 真 wire 提 messages（不再假定顶层 `id`，`stableKey` 加 idx 兜底 + 位置键）；⑤ **无顶层 id → 位置键** — 同 `c6d2f90`，message id 从位置而非字段取；⑥ **cwd 编码** — `cc00a3f` 实证 pi `session-manager.js` 真算法（`path.resolve` → 去单开头分隔符 → `/` `\` `:` 映射 `-`），bridge 占位实现 `encodeURIComponent().replace(/%/g,'')` 与真算法错位（已各自独立成条）；附记：`extractMessagesByKey` 与 `translateToPiWire` 互为上下游，wire 真形一次性全栈对齐。
  - **实现遗漏（2 问题 / 2 commit）**：⑦ **握手 `get_state` 写入缺失** — `52557fb` spawn 后从不写 `get_state` → pi 静默等输入、bridge 等响应死锁（已随 spawning 修复流转；任务 04 实现遗漏）；⑧ **worker `routeOpenMessage` 加 default 转发** — `1c86aca` M3 新 type（9 pi + `control/get_state`）在 worker DO 路由 `routeOpenMessage` 无 default 转发分支会被静默丢弃；**注：任务 01 review 曾预警"破锁后新增 type 需走 worker 转发链"被误放行**，本轮修复同步在最近变更里点出。教训：协议破锁（ADR-0006）增 `get_state` 时未联动 worker 转发链。
  - **生命周期/竞态（3 问题 / 3 commit）**：⑨ **snapshot 不稳定** — `e19a687` web `useSyncExternalStore` 每次 render 返回新对象 → React 死循环崩溃；⑩ **StrictMode 双跑 dispose 竞态** — `1f7679b` 恢复仪式中 `dispose()` 在双挂载下与新一轮 `initiate` 竞态，迟到 cancel 让在途 reply resolver 走 request_expired 而非真已过期；⑪ **child error 裸奔** — `52557fb` 同条三连根因之一（已随 spawning 修复流转）；修复点同时落到 [[architecture/decisions/0003-session-lifecycle-and-history-source.md#补注child-error-事件处理策略2026-09-07|ADR-0003 child error 补注]]。
  - **状态机语义（1 问题 / 1 commit）**：⑫ **读操作误转 running** — `3757ee2` `completeHandshake` 内部对 `get_messages` 走 "状态机写入"，与 PRD §2.7"读不转 phase"语义冲突，已收敛到"仅写命令驱动 phase 迁移，读命令永远只读"。
  - **教训一句**（与既有条目呼应不重复展开）：12 个问题里 wire/落盘细节 vs pi 实物这一类型仍是主力——见原 `testing.md` §1.3（2026-09-08 拆解、内容承接至 [[architecture/decisions/0008-fake-llm-isolated-pi-integration-tests.md|ADR-0008]] §关键 wire 发现 + §升级体检仪式）已挂账 6 笔（cwd 编码 / 翻译层字段名 / error 形状 / 握手写入缺失 / spawn cwd 缺失 / child error 裸奔）+ 本轮补 2 笔（pi 裸事件对象分发 `3049567`、web 提取器位置键 `c6d2f90`），共 **8 笔** 落在这条铁律之下；**凡未实测的 wire / 落盘细节均不可信**这条铁律（cwd 修复条尾部 / bridge→pi 翻译层修复条尾部均有同一表述）经 6+ 笔独立 commit 反复验证，不再独立成段。
  - **手测现状一句话**：**多轮对话闭环已通**——"能正常回复并进行多轮对话了"（用户 2026-09-07 手测反馈，cwd 编码修复后）；其余 14 条验收清单（abort / 4 类弹窗脚本化触发 / timeout 自答 / 多 web / bridge 重启自愈 / 线上部署）依赖原 `testing.md` §2 假 LLM 集成测试基建（2026-09-08 承接至 [[architecture/decisions/0008-fake-llm-isolated-pi-integration-tests.md|ADR-0008]]）或后续手测轮次，按现状挂账。
  - **任务 08 状态**：保持 `doing`（用户未逐条勾完 14 条手测清单），文档侧本轮把上述 7 笔 hotfix 收口记录归档；本条作摘要汇总，**不**逐条展开（部分问题已有独立最近变更条目）。
- 2026-09-07（测试规划文档落盘 — 原 `testing.md` 新建） — 用户裁定（2026-09-07）暂不实施假 LLM 集成测试基建、先落文档。原 `testing.md` 四节：(1) **测试分层现状** — shared 83 schema / bridge ~186 FakeChild 单测 / worker 与 web 零测试基建 / 真 pi 仅用户手测 + 一次性 /tmp 探针；M3 联调 12 问题中 6 个源于"文档假设 vs pi 实物"偏差，详见 [[current-state.md#最近变更]] 2026-09-07 cwd 修复 / bridge→pi 翻译层 / spawning 死锁三条。(2) **已挂账方案：假 LLM server + 隔离 pi 集成测试**——机制（隔离目录四件套 models.json / settings.json / auth.json + `PI_CODING_AGENT_DIR` + `PI_OFFLINE=1`，含完整示例 JSON），**格式裁定 Anthropic Messages API**（用户裁定替代 scout 报告中的 OpenAI 格式；POST `/v1/messages` + SSE 事件序列 `message_start` → `content_block_start` → `content_block_delta(text_delta)` → `content_block_stop` → `message_delta` → `message_stop` + `x-api-key` + `anthropic-version` 头），实施时核实点（pi-ai anthropic-messages contract 是否同样尊重自定义 baseUrl / `api` 字段 anthropic 取值以 docs/models.md 为准——该文档待建），覆盖范围（流式解析 / session 落盘 / `--session` 恢复 / idle kill 唤醒链 / 脚本化 4 类 extension_ui_request 弹窗——解决"手头无触发插件"痛点），CI 门控（CI 跳过 / 单独 `test:integration` 脚本 / fixture port 0 随机 + 进程内 http server 80-150 行）。(3) **展望：无头浏览器 E2E**——Playwright 驱动 wrangler dev + bridge + 真 pi + 假 LLM，简要一段细节待讨论后补充。(4) **升级体检仪式**——pi 升级时手工走 6 项（握手响应 / 事件名 / 落盘编码 / extension_ui_request 方法清单 / 失败响应 error 形状 / idle-kill 行为），本轮 M3 联调 6 个 wire 硬伤的针对性防御。TODO / 阻塞区同步挂账（详见上方）。本条作摘要汇总；不增改既有最近变更条目。
- 2026-09-07（M3 cwd 编码修复 — commit `cc00a3f`，bridge 测试 269 → 281 全绿） — 用户实测确认：**idle 5 分钟 kill 后再对话接不回同一 session（每次都是新的）**。**根因**：bridge 的 `encodeCwdForPi` 占位实现（`encodeURIComponent(cwd).replace(/%/g,'')`）与 pi 真实算法（`session-manager.js` 的 `getDefaultSessionDirPath`：`path.resolve` → 去单开头分隔符 → `/` `\` `:` 映射为 `-`）**不一致**——以 `work_dir = /home/sankabox` 为例，bridge 扫 `--2Fhome2Fsankabox--`（磁盘不存在），pi 写/读 `--home-sankabox--`（磁盘实存 7 个会话文件）；扫描落空 → spawn 不带 `--session` → 每次都是新会话（5min idle kill 后恢复仪式也救不回）。**修复**：3 行精确转写 pi 真实算法 + 测试翻转（旧断言钉的是错误形状）+ 地面真值回归（断言编码命中磁盘真实目录，宿主无该目录则跳过）。**任务 04 挂账的"cwd 编码真实 pi 比对回归"实测完成比对——占位实现被否决、pi 真实算法转写为正，本轮同步清账**。**教训一句**：任务 04 的对比回归项拖到联调才暴露，**凡未实测的 wire/落盘细节均不可信**——bridge→pi 翻译层此前同样踩坑（字段名错位存活到联调，见上一条），与本条属同一类型缺陷。详见 [[tasks/m3/04-bridge-pi-process.md#勘误注记2026-09-07|tasks/04 勘误注记]] + [[prds/m3-single-session.md|M3 PRD]] 顶部 2026-09-07 修订注记；本条作摘要汇总，正文不回改（依项目惯例）。
- 2026-09-07（M3 bridge→pi 翻译层两笔修复 — `837d1de` + `44960b9`） — 用户手测发现 prompt 命令在 pi 内部抛 `TypeError: Cannot read properties of undefined (reading 'startsWith')`，根因 2 处：(1) **bridge→pi 命令帧字段名错位** — pi 0.85.1 RPC schema 要求 `prompt` / `steer` / `follow_up` payload 用 `message` 字段（`rpc-types.d.ts` 实证），bridge 误发 web wire 的 `content` → pi 读 `undefined` → `.startsWith` 崩；新增 `translateToPiWire` 单点翻译（`DeferredCommand` 持有 web wire，写 stdin 时翻译，未来字段再变只需改一处）。(2) **bridge→pi 失败响应 `error` 形状归一化** — pi 0.85.1 RPC 失败响应 `error` 恒为 **string**（`rpc-mode.js` 工厂 + `rpc-types.d.ts` 双证），bridge 原样透传被 worker `command_result.error` schema（要求 `{code, message}` 对象）拒绝 → 任务失败 UX 路径。已补 `normalizePiError` 六分支（`string` / `Error` / `{code,message}` / `{message}` / `null` / 缺省）统一归一为 `{code: 'pi_error', message}`。**附记 `get_messages.since`**：web wire 保留 `since` 以备 M+ 切换 `get_entries` 时复用；pi 的 `get_messages` 不接受 `since`（属 `get_entries`），`translateToPiWire` 在写 stdin 时直接丢弃（stderr 不告警，属"web wire 字段在 bridge→pi 单向不可用"的同类处置）。**测试 259 → 269**（+10）：Section 13 wire 保真 10 条（`toEqual` 正向 + `not.toHaveProperty` 反向）+ 整帧 `safeParse` 回归 10 条。**历史教训**——旧测试 §1.3 长期断言错误 wire 形状（`content`），与 pi 真 schema 错位却仍绿，使字段名 bug 存活至今；本次同步将断言改为正向 `message` + 反向 `not.toHaveProperty('content')`。权威实现注记见 [[architecture/protocol/pi.md#bridge→pi-翻译层实现注记|architecture/protocol/pi.md §bridge→pi 翻译层（实现注记）]]；本条仅做摘要，不在 ADR-0003 重复（避免双真相源）。
- 2026-09-07（M3 spawning 死锁修复 + bridge 行为变更 — commit `52557fb`） — 用户手测发现 bridge 卡死 spawning（web 侧恢复失败）。**三连根因**：(1) **握手写入缺失**——`spawnNow` 从不写 `get_state` 到 pi stdin（[[prds/m3-single-session.md|M3 PRD §2.3]] 第 2 步要求），pi 静默等输入、bridge 等响应，死锁；已补 `writeHandshakeGetState()`（spawn 后立即同步写 + outstanding 表登记 `bridgeInitiated`）。(2) **spawn cwd 缺失**——`PiSpawnOptions` 无 `cwd`，pi 跑在 bridge 进程目录，session 落盘目录（`--<实际cwd>--`）与 bridge 扫描目录（`--<work_dir>--`）错位；已补 `cwd: workDir` 传递链。(3) **child error 裸奔**——无 handler 时 `ENOENT` 等会触发 `uncaughtException` → bridge `exit(1)`；已挂 handler → `exited` 广播 + 队列丢弃 warn + **永不自动重启**（持续性错误无退避机制，避免死循环；下次 spawn 触发集命令再唤醒，见 [[architecture/decisions/0003-session-lifecycle-and-history-source.md#补注child-error-事件处理策略2026-09-07|ADR-0003 child error 处理策略补注]] + §2.7 语义）。**review 修复**：(W-1) **反向事件序竞态**——`exit` 先 / `error` 后会误杀重启后的新 spawn，用 **child 身份比对**守卫（当前 `child === this.child` 才走 exit/error 处理）规避；(S-7) **`bridgeInitiated` get_state 失败响应外泄**——命中后无论成败直接消费 return，避免冒泡到 outstanding 表。**实测确认**：pi 0.85.1 握手响应形状与 bridge 解析完全匹配（2ms）；"首组 setStatus 帧"实际形态是 `extension_ui_request{method:"setStatus"}`（vimmode 插件噪音；type filter 行为正确，仅注释名实不符）。**bridge 测试 243 → 247**（+4：handshake 写入断言 + cwd 传递 + child error handler + exit/error 竞态守卫），工作区全绿。**事实修正**：PRD / 任务 04 / roadmap §4.2 中"首组 setStatus 帧"实际是 `extension_ui_request{method:"setStatus"}` 信封，type filter 行为正确，**只是注释里"setStatus 帧"的说法名实不符**（fire-and-forget 5 类之一 `setStatus` 与同名扩展 UI 方法同字；扩展 UI 方法都走 `extension_ui_request` 信封，不是顶层 `setStatus` 事件）。代码侧 commit `52557fb`，task 08 文档补注与三端联调手测交接见 [[tasks/m3/08-docs-and-validation.md#用户手测交接|tasks/08 用户手测交接]]（**本修复改变了 bridge 行为**——若此前测试中 bridge 已处于卡死状态，需重启 bridge 进程后再测）。
- 2026-09-05（M3 去隔离改造落盘 — 用户裁定 → 代码 → ADR-0007 → 文档链） — [[architecture/decisions/0007-host-shared-pi-agent-dir.md|ADR-0007]] 落盘：bridge 改为完整复用宿主机 pi 环境，session 扫描基准 = `resolvePiAgentDir()`（与 pi `getAgentDir()` 语义一致：env 优先 + tilde 展开 + 默认 `~/.pi/agent`）；code commit `1985fbd`（spawn 去注入 env + auth 缺失仅 stderr warn）。已接受的设计后果：①“取最新”作用于共享池，web 接管该 `work_dir` 最近会话（可能正是用户终端正在聊的）；②同会话双写风险——bridge pi running 时用户又启交互 pi 写同一 session → 两进程写一份 jsonl，M3 单用户以行为约定规避，M4 可加“会话被外部占用”检测。文档链：[[architecture/decisions/0003-session-lifecycle-and-history-source.md|ADR-0003]] 末尾追加“去隔离改造”补注 + 相关条目加 ADR-0007；[[prds/m3-single-session.md|M3 PRD]] 顶部「修订注记」加 2026-09-05 条，§2.3 / §2.5 / 用户操作清单保留作历史；[[getting-started.md|getting-started §3.5]] 大幅简化（删除 auth.json 复制 + `PI_CODING_AGENT_DIR=<configDir>/pi-agent` 指引，改为“无额外配置 + 缺失时 stderr 提示 + `pi auth check` 验证 + `PI_CODING_AGENT_DIR` 高级覆盖”）；[[tasks/m3/09-host-shared-agent-dir.md|tasks/09]] 事后补录（done）。详见 [[tasks/m3/09-host-shared-agent-dir.md#完成情况|tasks/09 完成情况]]。
- 2026-09-05（修正 `pi login` 错误表述 — 用户实测反馈 + scout 实证 pi 0.85.1 无此命令；docs/ 三处同步 + bridge 提示文案 + 测试） — pi 0.85.1 **没有** `pi login` 顶层命令（子命令仅 `install` / `remove` / `uninstall` / `update` / `list` / `config` / `auth`，`auth` 只读）；登录入口是 pi TUI 内斜杠命令 `/login`（可带 provider 名），凭证写入 `getAgentDir()/auth.json`（权限 0600）；本机目录解析 `PI_CODING_AGENT_DIR` env 优先，否则 `~/.pi/agent`。
  - **[[getting-started.md|getting-started §3.5]]**：原"提醒用户手动跑一次 `pi login`" + bash 代码块改为准确操作指引——两种方式二选一：① `PI_CODING_AGENT_DIR=<configDir>/pi-agent pi` 进 TUI 后输入 `/login`（或 `/login <provider>`）跟提示完成 OAuth；②（**推荐，最快**）`cp ~/.pi/agent/auth.json <configDir>/pi-agent/auth.json && chmod 600 ...`。新增"pi 无 `pi login` 顶层命令"注意段（含子命令清单）+ 凭据优先级（auth.json > `*_API_KEY` env > `--api-key`，roadmap §4.8）+ `pi auth check` 验证手段。`grep "pi login" docs/getting-started.md` 全文仅余"pi 没有 `pi login` 顶层命令"否定上下文。
  - **[[tasks/m3/08-docs-and-validation.md|tasks/m3/08 §用户手测交接]] 前置准备第 2 步**：原"`pi login`"改为上述两种方式（推荐方式 ② 复制现有 auth.json）+ 提示 pi 0.85.1 无此命令 + 链 [[getting-started.md#3.5 配置文件 JSON 字段说明|getting-started §3.5]]。
  - **[[prds/m3-single-session.md|M3 PRD]]**：依项目惯例**不改定稿正文**，在文档顶部「修订注记」区追加一条（2026-09-05）—— §2.3 与「用户操作清单」中 `pi login` 表述有误（pi 0.85.1 无此命令），实际认证方式见 [[getting-started.md#3.5 配置文件 JSON 字段说明|getting-started §3.5]]（TUI `/login` 或复制 auth.json）；bridge stderr 提示文案已同步修正。
  - **bridge 代码侧**：stderr 提示文案去掉 `pi login` 字样，改为指引到 [[getting-started.md#3.5 配置文件 JSON 字段说明|getting-started §3.5]] 两种方式。
  - **测试侧**：`packages/bridge/src/index.test.ts` 两条环境敏感测试修复——XDG 隔离（避免污染本机 `~/.config/remotepi`）；默认路径存在合法配置时也全绿（既保留测试真实性又防止 CI/本机误删 `~/.pi/agent/auth.json` 等误伤）。
  - **顺带记录**：本修正由 [[tasks/m3/08-docs-and-validation.md|tasks/m3/08]] 文档部分补交（任务 08 仍 `doing`，用户手测未完成）。
- 2026-09-05（M3 task 08 文档部分完成 — ADR-0003/0004 补注 + getting-started §3.5 + 挂账回收；三端联调手测移交用户） — [[tasks/m3/08-docs-and-validation.md|tasks/08]] 文档部分全量落地：[[architecture/decisions/0003-session-lifecycle-and-history-source.md|ADR-0003]] 末尾**4+1 段补注**——(1) 恢复措辞与"双查询恢复仪式"对齐（web 握手后并行发 get_messages + get_state，两条都到才渲染；exited 时 get_messages 触发带 --session 的 spawn；5s 超时含 spawn 余量，`REPLY_TIMEOUT_MS = 5000` 单点）；(2) idle 计时不被阻塞弹窗误触发（agent_settled 是判据；弹窗阻塞期间不发 agent_settled，与 ADR-0004 协同）；(3) 自主 kill 标记规则（先置位再发 SIGTERM→1s→SIGKILL；exit 回调三路径：标记在→不重启 / 标记不在+code≠0→崩溃重启 / 标记不在+code=0→exited 不重启，覆盖原"exit≠0→重启"未区分自主 kill 的语义盲区）；(4) exited spawn 触发集（prompt/steer/follow_up/get_messages 触发 spawn；get_state 永由内存作答；abort no-op）+ **(ready 阶段忽略 agent_settled 裁定)**——PRD §2.3 只定义 running→idle，ready=尚无工作量。[[architecture/decisions/0004-extension-ui-dialog-forwarding.md|ADR-0004]] 末尾**4+2 段补注**——(1) 弹窗模型升级为状态帧驱动（session_state.blocked_on 唯一真相源；web 端 extension_ui_request 事件仅信息性）；(2) 无乐观 UI 规则（本地操作→"已提交待确认"暂态→blocked_on 不含该 id 才收起）；(3) 并发多端"先答者胜"（迟到者收 request_expired）；(4) extension_ui_response web wire 形状（value: string|boolean；ConfirmDialog 实际落地三按钮 Cancel=cancelled:true / No=value:false / Yes=value:true，cancel 与 no 语义正交）+ **(ConfirmDialog 三按钮挂账)** + **(work_dir 降级挂账)**——PRD §4.3 要求显示 work_dir 但 v1 wire 不携带 bridge 配置（get_state.data 增 work_dir 属 envelope 演进 (a)，M4 候选）。[[getting-started.md|getting-started]] §3.1/§3.4 bridge 命令改 `bridge --config <path>`（默认 `~/.config/remotepi/bridge.json`）+ 新增 §3.5「配置文件 JSON 字段说明」（worker_url / web_base_url / work_dir / token 四字段 + XDG 解析 + token 缺省随机生成不回写 + work_dir 严格校验不 mkdir + 未知 flag 静默 + auth.json 提示）+ §10 末尾加注 + grep 验证 `REMOTEPI_WORKER_URL` / `--worker-url` 零残留。**3 处挂账回收**（已落到 ADR 补注）：① cwd 编码真实 pi 比对回归项（任务 04 移交）；② ConfirmDialog 三按钮模型（任务 06 移交）；③ work_dir 降级显示（任务 06 移交）。**任务状态**：08 文档部分 doing（任务文件末尾新增「用户手测交接」小节，14 条手测清单原文保留，**不勾选**——由用户在浏览器执行手测验收；任务 frontmatter `status` 保持 `doing` 等用户验收后由编排者改 `done`）。M3 整体文档收尾完成；仅余用户 14 条三端联调手测验收 + 用户手动 `git push origin main` 触发 CD。详见 [[tasks/m3/08-docs-and-validation.md|tasks/08 完成情况]]（待补）。
- 2026-09-05（M3 task 07 完成 — web 链 06→07 收官，M3 代码侧 01-07 全部 done） — [[tasks/m3/07-web-recovery.md|tasks/m3/07]] web 双查询恢复仪式全量落地：`recovery.ts` 新建（`RecoveryGate` + `initiateRecovery`：resolver 注册先于 send、per-reply 5s timer、stale 标志覆盖所有异步入口、三态 error `snapshot_failed`/`state_failed`/`both_failed`、retry + dispose 双路径）+ `WsClient.registerReplyResolver` 一次性 resolver 分流 + result 集中处理（`SessionStatePayloadSchema` 校验后同步 sessionPhase + blockedOn 到 store，避免 ready=true 但 store 仍空的撕裂态）+ App 三态渲染（`RecoveryInFlight` spinner / `ChatView` ready / `RecoveryErrorCard` 重试）+ F5 同路径（token 走 URL hash，无持久化）。reviewer **「需修复后通过」**；**5 Warning 全修**（token 切换重发仪式 / replyResolvers JSDoc 失真 / `setBlockedOn` 浅相等守护 / 4 处静默吞异常补 warn / `tryDecodeGetStateData` 共享解码）+ **4 Suggestion 落地**（useRef/useCallback 类成员 / ConnState 判别联合类型 / CSS selector 合并 / autoStartConsumedRef 改名）。web build **232 KB**（较 06 +4.4 KB）；工作区全量 **237 条测试不变**。**三个合理偏差**（reviewer 认可）：(1) result 集中处理保持 store 同步；(2) `connState` 门控防 send 静默丢；(3) `useRef` gate + `strictModeMountAbsorbedRef` 防 StrictMode 双挂载。**5s 超时含 exited spawn 余量说明**：PRD §非目标环形缓冲补发归 M+，5s 留给 web send → bridge 收 → spawn pi → 加载 → bridge 转发 → web 收 整链路 2-3s + 1-2s 余量；超时真触发 → RecoveryErrorCard 重试按钮，不会卡死或静默丢；常量集中在 `REPLY_TIMEOUT_MS = 5000` 单点。**Commit**：`cbeea2e`。**web 链 06→07 至此全部完成**（06 交付 ChatView/4 类弹窗/WsClient 状态机 → 07 交付双查询仪式/reply_to 分流/失败重试/F5 同路径 → 08 接手 ADR-0003/0004 补注 + 三端联调验收 14 条）。**M3 代码任务 01-07 全部 done，仅剩 08 文档收尾 + 用户手测**。详见 [[tasks/m3/07-web-recovery.md#完成情况|tasks/m3/07 完成情况]]。
- 2026-09-05（M3 task 06 完成 — web 链前半部） — [[tasks/m3/06-web-chat.md|tasks/m3/06]] web 聊天界面 + 4 类阻塞弹窗全量落地：WsClient 扩展（**6 出站方法** sendPrompt/sendSteer/sendFollowUp/sendAbort/sendGetMessages/sendExtensionUIResponse + pi 事件入站分流 `message_update`→streamingDraft / `message_end`→messages 收敛 / `agent_settled`→输入框可用 / `extension_ui_request`→blockedOn 入列 / `queue_update`→queue.steering+queue.followUp + WebState 5 字段 messages/streamingDraft/queue/sessionPhase/blockedOn + `command_error` 订阅）+ ChatView 主组件（PhaseIndicator / MessageList / QueueIndicator / InputBar）+ **4 类弹窗**（SelectDialog / ConfirmDialog / InputDialog / EditorDialog；**ConfirmDialog 三态按钮**——Cancel=cancelled:true / No=value:false / Yes=value:true，cancel 与 no 语义分离）+ **DialogHost** 状态机（open / submitting / expired 三态 + 关闭规则：点取消 / 提交成功 / 阻塞消失 / 提交失败）+ 无乐观 UI（H 决策：提交后"待确认"暂态，事件 / 状态帧驱动最终态）+ §4.5 命令失败 UX（dialog → toast + 自动收起；普通命令 → 输入框临时错误提示不重发）+ **M2 验证组件** BroadcastLog.tsx / PingTester.tsx 随本次清理删除（git 可恢复）。reviewer 通过，**无 Critical**；**3 项 Warning 全修**（§4.5 命令失败 UX / 本地超时分支改 toast / M2 死代码清理）；**4 项 Suggestion 落地**（message_end 按 id upsert / stableKey 加 idx 兜底 / 倒计时 1Hz 节流 / `submitText` 重构消除强转）。web build **227.6 KB**；工作区全量 **237 条测试不受影响**（web 包 M2 起无 vitest，沿用范围不变）。**Commit**：`073f4c6`。**两条挂账（移交任务 08）**：(1) **work_dir 降级显示**——PRD §4.3 要求 PhaseIndicator 显示 phase + work_dir，但 v1 wire 不携带 bridge 配置（work_dir 不过协议），实际仅显示 phase；`get_state.data` 增可选 `work_dir` 属 envelope 演进规则 (a)，不在 M3 范围，任务 08 在 ADR-0003/0004 补注或验收纪要中注明偏差；(2) **ConfirmDialog 三按钮表述漂移**——PRD §4.2 表隐含两按钮（确认/取消），实际落地三按钮（Cancel / No / Yes），"取消"与"否"语义分离是正确设计（cancelled 与 value 正交），任务 08 同步 pi.md / ADR-0004 时把三按钮模型写清。详见 [[tasks/m3/06-web-chat.md#完成情况|tasks/m3/06 完成情况]]。
- 2026-09-05（M3 task 05 完成 — bridge 链 03→04→05 全部完成） — [[tasks/m3/05-bridge-popup-core.md|tasks/m3/05]] bridge 弹窗核心全量落地：`extension-ui.ts` 新建**独立 `ExtensionUIRouter` 类**（职责分离：manager 只管相位状态机、router 只管 pending Map + timeout 镜像 + wire 翻译，通过 5 个回调交互，可纯单元测试）+ `pi-process.ts` 接入（任务 04 的桩 `extensionUIRouter!: ExtensionUIRouter` 替换为真构造 `buildExtensionUIRouter(options)`，广播原则落地：`broadcastSessionState` 读 `router.getBlockedOn()` 组装 `session_state.payload.blocked_on`，仅由写操作 / 相位迁移 / blocked_on 变化触发，`get_messages` 永不发广播）+ `client.ts` 6 命令转发 + control `get_state` 内存回答（不 spawn）。弹窗核心要点：**4 类入列**（select/confirm/input/editor）+ **5 类 fire-and-forget 消化**（notify/setStatus/setWidget/setTitle/set_editor_text）+ **timeout 镜像 + 5 处竞态原子检查**（`pending.get` + `pending.delete` 配对、`timeouts.delete` 同步；输家走 no-op）+ **wire 翻译三态**（`cancelled:true` 不附 value / confirm `value:boolean → confirmed` 路径含 `value:false → confirmed:false` 独立用例 / select/input/editor `value:string → value`）+ **多 web 端先答者胜**（广播驱动 + 后续答者收 `request_expired`，与已 clear 的 id 不重复）+ **`clearAll` 防 ghost dialog**（child exit / `stop()` 路径清空 pending + timeouts + 单次终态广播）。reviewer 通过，**零 Critical 零 Warning**；**5 项低成本 Suggestion 全修**——S1 duplicate id 告警 / S2 翻译 null 分支补 `invalid_response` 回执（让 web 走 §4.5 提交失败 UX，不再静默吞）/ S3 §11 fake timers / S4 §11.7 stdin 端到端断言（mock write bytes + 断言广播 payload 含已清空 blocked_on）/ S5 `writeToPi` 同步契约 JSDoc（钉死 boolean 同步返回是 race 原子性的关键）。bridge 测试 **106 → 154 条**（新增 47 条），工作区全量 **237 条全绿**。**Commit**：`a466188`。**§6.2 清单覆盖**：03 已转测配置加载 / CLI 移除回归；04 已转测 session 扫描 / 状态机 / 自主 kill 三路径 / exited 触发集 / get_state 不 spawn；**05 承接后半部**——blocked_on 维护（§1-§2 = 12 条）/ wire 翻译（§3 = 9 条含 S2 invalid_response 兜底）/ 多 web 先答者胜（§4 = 4 条）/ 广播原则端到端（§5 + pi-process §11 = 12 条）/ stdin 写失败 + clearAll + envelope round-trip（§6-§9 = 10 条）。**结构裁定**：`ExtensionUIRouter` 独立类的四大理由——职责分离（manager / router 互不泄漏内部状态）/ 可纯单元测试（`makeRouter()` 工厂注入 5 mock 回调，不构造 manager、不 mock spawn）/ 便于 M4 多 session 按 sessionId 分桶替换 / `writeToPi` 同步 boolean 契约钉死未来重构。详见 [[tasks/m3/05-bridge-popup-core.md#完成情况|tasks/m3/05 完成情况]]。
- 2026-09-05（M3 task 04 完成） — [[tasks/m3/04-bridge-pi-process.md|tasks/m3/04]] bridge pi 子进程状态机落地：`pi-process.ts`（922 行，5 相位状态机 / 启动握手 / 5min idle kill SIGTERM→1s→SIGKILL / **自主 kill 标记** / exit 三路径 / session 目录扫描 / **outstanding 命令表**）+ `pi-cwd-encoder.ts` + `client.ts` 最小接线（onEnvelope sink）+ `index.ts` 组装（`PI_CODING_AGENT_DIR=<configDir>/pi-agent/`，auth.json 缺失 stderr 提示）。reviewer 通过，**无 Critical**；7 项 Warning 全修，**含两条 wire 硬伤**——`get_messages` 回执改走 `snapshot` envelope（而非误用 `command_result`）+ `reply_to` 改为回显 web 命令 id（outstanding 表反查；原实现为 `command_result + 随机 UUID`，会**破坏任务 07 双查询恢复仪式**的请求-回执配对，已修）；其余 5 Warning：idle timer 守门 running / **ready 阶段忽略 `agent_settled`** 裁定（PRD §2.3 只定义 running→idle；ready=尚无工作量）/ auth.json 提示切 stderr / `since` 透传 + TODO(M+) / `extension_ui_response` 桩 TODO(task 05)。6 项 Suggestion 落地（U+2028 断言加强 / onEnvelope+wiring 测试 / blocked_on 缺省断言 / WRITES-READS 常量 / 崩溃丢队警告 / 文件名时间戳形态校验）。bridge 测试 **41 → 87 条**，工作区全量 **189 条全绿**。**Commit**：`75fad56`。**待办移交**：`encodeCwdForPi` 真实 pi 比对回归项留 [[tasks/m3/08-docs-and-validation.md|tasks/08]]；`extension_ui_response` web 入站翻译留 [[tasks/m3/05-bridge-popup-core.md|tasks/05]]。详见 [[tasks/m3/04-bridge-pi-process.md#完成情况|tasks/m3/04 完成情况]]。
- 2026-09-05（M3 task 03 完成） — [[tasks/m3/03-bridge-config-file.md|tasks/m3/03]] bridge 配置全量迁移落地：`config.ts` 新建（zod strict + work_dir 三连校验不 mkdir + XDG 可选 + token 不回写）、`index.ts` 重写为配置驱动、`token.ts` `shareUrl` base 必填、`peerDependencies["@earendil-works/pi-coding-agent"]` 与 `peerDependenciesMeta` 整段删除。reviewer 通过，**无 Critical**；3 项 Warning 全修（options.token 注入时 shareUrl 契约 / `logger.error` 切 stderr / `--config` 遇 `-` 开头不误吞），另补 `--` 终止符停止扫描 / ConfigError 保留 `cause` / strict schema camelCase typo 拒测试。bridge 测试 **41 条全绿**（19 既有 + 22 新增），tsx 实跑冒烟通过。**Commit**：`3fbd93e`。**行为变更注记**：`logger.error` 由 stdout 切 stderr（M2 时代 `logger.info` / `logger.error` 同流，运维不便按流分离；本任务统一修正，`logger.info` 保持 stdout）；bridge 直接依赖 `zod ^3.24.1`（与 shared 同版本，config schema 仅 bridge 消费）；`--config` 遇 systemd 风格 `-D` 不误吞、遇 `--` 终止符停止扫描。详见 [[tasks/m3/03-bridge-config-file.md#完成情况|tasks/m3/03 完成情况]]。
- 2026-09-05（M3 task 02 完成） — [[tasks/m3/02-shared-tests.md|tasks/m3/02]] shared 测试扩展落地：**4 文件 61 条新增用例**（超 PRD §6.1 25 条下限），review 后补 steer/follow_up 缺 content 镜像 2 条，套件总数 102 条全绿（22 既有 + 80 新增）。reviewer 通过，**无 Critical**；3 项 Warning 中 W-1 / W-3 已修，**W-2 挂账两处 schema 行为偏差**，详见 [[tasks/m3/02-shared-tests.md#完成情况|tasks/m3/02 完成情况]]：(1) `block-on.test.ts` case 11 — `EditorBlockedOnEntry` schema 无 `.strict()`，zod 默认剥离未知键，PRD §1.3 未列 `timeout` 字段故未拒（收紧属 wire breaking，需 envelope 演进规则 (d) 配套修订）；(2) `get-state.test.ts` case 9 — `result.error` 当前 optional，`ok:false` 缺 error 实际通过（收紧需 `discriminatedUnion('ok')` 重构，同属 wire breaking）。后续任务 04 / 05 消费 `blocked_on` / `result.error` 时需知晓此两处行为。**Commit**：`9bc5bad`。
- 2026-09-05（M3 task 01 完成） — [[tasks/m3/01-shared-protocol-v2.md|tasks/m3/01]] shared 协议 v2 落地：**pi 家族 9 schema 真化**（`z.never()` 占位 → `discriminatedUnion('type', [...9 pi envelopes])`，命令 6 + 回执/事件 3 全 schema）；**control 9 type 破锁**（8 → 9，新增 `get_state`，回执走 `result`，依据 [[architecture/decisions/0006-protocol-v1-get-state-unlock.md|ADR-0006]]）；**`session_state.payload.blocked_on`** 可选数组（4 类 `extension_ui_request` 阻塞形状，与 `result.data` 同形）；**`extension_ui_response` web wire refine**（`{ request_id, cancelled, value?: string|boolean }` + refine，confirm 走 boolean 表达"否"）；**协议四文档同步**——[[architecture/protocol/envelope.md|envelope.md]] 锁版 control 8→9 type + 演进规则 (a)(b) 修订 + 与 ADR-0006 双向引用；[[architecture/protocol/control.md|control.md]] §5 blocked_on 增补 + 新增 §6.5 get_state + type 列表 9 + 中间层加 get_state 转发 + README 同步 9 type；[[architecture/protocol/pi.md|pi.md]] 9 type + `extension_ui_request` id 稳定注记删除 + web wire 形状 + 5 个常见事件名；ADR-0006 删除占位行 + 双向引用段补全。**关键偏差**：(1) 实际多拆出 `literals.ts`（所有 `as const` 字面量 + 推导 type，无协议内导入的叶子模块）与 `envelope-base.ts`（`VersionLiteral` / `EnvelopeBaseControl` / `EnvelopeBasePi` 头部）两文件——`envelope.ts ↔ control.ts` / `envelope.ts ↔ pi.ts` 在 M3 拆分后形成循环导入，叶子抽取是打破循环的必要手段；`envelope.ts` 退化为"装配点"（re-export literals + 顶层 `Envelope` z.union）。(2) `packages/shared/package.json` 补 `typecheck` 脚本（与 `build` 等价的 `tsc --noEmit`，便于 CI 按用途分阶段验证）。**验证**：shared build/typecheck/test 全绿，`pnpm -r build` / `pnpm run lint` / `pnpm run typecheck` 全绿；`grep "PiBranch = z.never()"` / `grep "HelloEnvelope|EchoEnvelope"` 均零结果（占位/M1 雏形已清理）；`grep "get_state" envelope.ts` 命中模块头注释 + literals.ts `CONTROL_TYPES`（由 envelope.ts re-export，语义等同 `CONTROL_TYPES` 定义本身）。**Review**：无 Critical；1 项 JSDoc Warning 已修。**Commit**：`6568b4b`。
- 2026-09-05（M3 PRD 定稿 + 拆任务） — [[prds/m3-single-session.md|M3 PRD]] 单 session 闭环定稿落盘（pi 子进程 + web 弹窗 + 状态恢复），任务拆至 [[tasks/m3/01-shared-protocol-v2.md|tasks/m3/01]] ~ [[tasks/m3/08-docs-and-validation.md|08]]（8 个；01 根；06/07 与 03/04/05 两链并行；08 收尾）。本轮敲定的关键决策：(1) **配置全量迁移**——bridge 仅保留 `--config <path>` 单 flag，配置文件 `~/.config/remotepi/bridge.json`（worker_url / web_base_url / work_dir / 可选 token 不回写）；`--worker-url` / `REMOTEPI_WORKER_URL` / peerDep `@earendil-works/pi-coding-agent` 全删（bridge 仅 spawn 外部 pi 二进制，0.85.1 基线不声明包依赖；会话发现走目录扫描不用 SessionManager）；(2) **协议破锁**——control 8 → 9 type（新增 `get_state`，回执走 result），演进规则 (b) 改"除 get_state 外不再新增"，记入 [[architecture/decisions/0006-protocol-v1-get-state-unlock.md\|ADR-0006]]；(3) **blocked_on 状态帧**——session_state payload 增可选数组（pi 原生 extension_ui_request 4 类阻塞形状；fire-and-forget 5 类不入列、bridge 本地消化不转发）；(4) **双查询恢复仪式**——web 握手后无 ack 即并行发 get_messages + get_state，两条都到才渲染，F5 同路径；exited 时 get_messages 触发带 --session 的 spawn（spawn 触发集 = prompt/steer/follow_up/get_messages；get_state 永由内存作答）；(5) **无乐观 UI**——状态帧唯一事实源，提交后"待确认"暂态，blocked_on 不含该 id 才收起，迟到提交收 request_expired；(6) **timeout 镜像契约**——pi 超时自答 stdout 零输出（0.85.1 实证），bridge 镜像定时器移出 blocked_on 并广播；(7) **extension_ui_response web wire**——`{request_id, cancelled, value?: string|boolean}`，confirm 走 boolean（value:false 表达"否"，修正原方案无法表达 confirm"否"的缺陷）；(8) **自主 kill 标记**——idle 超时 kill 不再被误判为崩溃重启（SIGTERM/SIGKILL 退出码非零；标记先置位再发信号，exit 回调三路径）；(9) 广播只由写操作触发；(10) worker 零代码改动。另记 TODO：将来恢复 CLI/env 支持。
- 2026-09-05（M2 线上验收完成，里程碑收官） — push dfbfe7b 后 CI/CD 首跑：CI 竞态（build 并发下 assets 目录未就绪）与 CD 凭据注入（init 阶段缺 AWS env）两处失败已修（f36b6db / 896214a），二次 push 全绿。deploy-worker 上传 Worker+Static Assets 成功，terraform-apply 完成 route 切换（remotepi-hello → remotepi-worker），remote-pi.sankabox.com 主域上线（SPA + /web /bridge WSS + /healthz）。用户浏览器验收通过；remotepi-hello 已删。可选加固（workers_dev=false、旧二进制清理）用户暂缓。M2 里程碑收官，下一步 M3（单 session 信息往来）。
- 2026-09-05（部署形态改定：合并主域 + CD 上 Actions） — 用户改定：网页并入 remote-pi.sankabox.com 主域（Worker Static Assets 托管 SPA，`/web` `/bridge` `/healthz` run_worker_first；`GET /` 让位网页首页，hello 挪 `/healthz`），Pages 项目与 `web.` 子域方案作废（TF 文件删除，未曾 apply）；bridge 分享 URL 改主域。CD：新增 deploy.yml，`push main` 自动 build web → wrangler deploy → terraform apply（secrets 待用户配置：`CLOUDFLARE_API_TOKEN` / `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`）。PRD §1#5 / §5 与 getting-started §10 已附修订注记。新增 [[architecture/decisions/0005-unified-domain-with-worker-static-assets-and-actions-cd.md\|ADR-0005]] 记录该部署决策。
- 2026-09-05（bridge 本地联调踩坑修复） — 用户实测发现按文档启动的 bridge 默认连生产域（wss://remote-pi.sankabox.com/bridge），本地 DO 房间无桥（bridge_status online:false、手动 ping 全超时）。根因：默认 URL 指生产 + getting-started 命令缺 --worker-url + 启动/断连日志不含目标 URL。修复（1410e60）：bridge 解析链加 REMOTEPI_WORKER_URL（flag > env > 默认）、启动打印 worker URL 与生产默认 hint、断连日志带 close code/reason、onerror 不再静默；getting-started §3.4 命令更正（上一次的 docs 更正被并行 revert，本次按实机输出重写）。
- 2026-09-05（M2 握手 bug 修复） — 用户本地联调发现浏览器报 ws connection failed 但 wrangler 侧 101 正常。裸 HTTP 探针坐实根因：workerd 对 101 不回显 Sec-WebSocket-Protocol，浏览器/undici 按 RFC 6455 强校验判失败（服务端 socket 已建，帧发进虚空）。修复：Room 的 101 Response 显式回显 remotepi.v1（workerd 支持 responseInit.headers，7eb1f0a）；新增 worker/scripts/probe-handshake.mjs 裸握手探针。回归：/web 与 /bridge 双端握手 + bridge_status 补发均通过。
- 2026-09-05（.vscode 精简） — 用户裁定移除 `.vscode/tasks.json`（不需要 Run Task 起服务与「三端全起」），保留 `launch.json` 三配置并改为自包含：`web: debug (chrome)` / `worker: attach inspector` 不再 `preLaunchTask`。dev 服务先按 [[getting-started.md|getting-started §3.4]] 在三个终端手动起，再走 §4 F5 调试。M1 历史条目（如 [[prds/m1-infrastructure.md#11-本地调试与一键启动vscode|PRD §11]] / [[tasks/m1/01-monorepo-scaffold.md|tasks/01]]）中"四件套 / tasks.json 四任务"为当时状态描述，**不回改**（历史记录）。
- 2026-09-05（M2 三端代码完成） — 任务 03/04/05 完成并通过跨组件联审（reviewer：无 Critical；修复 nonce 升 full UUID、bridge pong 严格配对、判死 close 1000、HANDSHAKE_TIMEOUT_MS 归位等）。bridge 6d58727 / worker b912a5a / web 1c252d8。根级 lint/typecheck/test/build 全绿（shared 22 测试 + bridge 32 测试）；worker bundle 138.62 KiB。待办：任务 06 infra 与 getting-started 落地后，用户本地三端联调手测。
- 2026-09-05（M2 PRD 定稿 + 拆任务） — [[prds/m2-tunnel.md|M2 PRD]] 按协议 v1 修订定稿：M2 实现范围收敛为 control 5 type（handshake/ping/pong/bridge_status/error），session_state/session_list/result 与 pi 家族不在 M2；信封按 kind 二分两层 discriminated union（pi 空位 z.never() 占位）；bridge 握手不等 ack（bridge_status 只发网页）、token 只走 subprotocol 不进 query、web WSS 连 worker 域；DO 主动探活作为 control.md §2 兼容补充落备注。拆 6 任务，03/04/05 可并行；交付约定本地 commit 不 push。
- 2026-09-05（协议 v1 定稿） — [[architecture/protocol/README.md|隧道协议]] 经多轮评审定稿并落盘 README / envelope / control / pi 四文档。要点：kind 二分为 control（连接与会话生命周期）/ pi（对话内容）；reply_to 仅限回执类（control 的 result、pi 的 command_result 与 snapshot）；不设 bye（离线不区分正常关闭与崩溃）；session_state 按会话一条（一个 bridge 可对应多个 pi 进程）；session_list 归 control、列表项带 running 简化状态；error 定向发送、terminal 标志、关闭码 1008；网页渲染规则（message_end 快照为权威、agent_settled 收尾）写进协议；评审中裁撤 capabilities 与 idle_remaining_s。v1 锁版承诺见 envelope.md。
- 2026-09-05（M1 真实环境闭环） — 用户本地完成 wrangler deploy（改用 CLOUDFLARE_API_TOKEN 环境变量认证）+ `terraform apply`（DNS + workers route 创建成功）；`curl https://remote-pi.sankabox.com/` 实测返回 `hello from remotepi worker v1`。**M1 里程碑完整收官**。顺带：worker `deploy` script 更名 `deploy:cf`（避开 pnpm 内置 deploy 命令），新增 `infra/terraform.tfvars.example` 并修复 `.gitignore` 行内注释失效（`*.tfvars` 此前实际未被忽略）。
- 2026-09-05 — task 05 完成，修复 `pnpm/action-setup` 双重版本指定后 aa87770 双 workflow 全绿。**M1 代码侧收官**。
- 2026-09-05（task 05 CI 首跑 + 修复） — CI workflow 首跑：terraform workflow 绿；ci workflow 红于 `pnpm/action-setup@v4` 同时收到 `version` 输入与根 `package.json` 的 `packageManager` 字段，触发 "Multiple versions of pnpm specified"。裁定 **pnpm 版本唯一真相源为根 `package.json` 的 `packageManager` 字段**，workflow 不再传 `version` 输入（v4 action 二者并存必报错），改为让 action 从 `packageManager` 自动解析。修复后重推 ci workflow 验证绿。同步落 [[prds/m1-infrastructure.md#8-github-actions-ci-骨架|PRD §8]]：`ci.yml` 示例 YAML 的 `- uses: pnpm/action-setup@v4` 行去掉 `with: { version: 10 }`、只保留注释说明；§8 缓存 bullet 后新增 "pnpm 版本唯一真相源" bullet；验收条目的 `version: 10` 表述改为"读 `packageManager` 字段定位 pnpm 版本"。
- 2026-09-04（task 03/04 完成 + review 通过） — [[tasks/m1/03-hello-worker.md|03]] / [[tasks/m1/04-terraform-cloudflare.md|04]] 完成并 review 通过：
  - **03 hello worker**：`worker/src/index.ts` 返回文案与 [[getting-started.md|getting-started §3.3]] 描述对齐（`hello from remotepi worker v1`，`PROTOCOL_VERSION=1` 来自 `@remotepi/shared`）；`wrangler dev` 本地起 `http://localhost:8787/`、curl 实测响应一致；`wrangler deploy --dry-run --outdir=dist` 产出 `dist/` 无错；`compatibility_date="2025-09-01"`、`name="remotepi-hello"` 与 [[prds/m1-infrastructure.md#7-terraform-细节|PRD §7]] route 引用对齐。
  - **04 Terraform CF**：`infra/` 全套到位（`versions.tf` / `providers.tf` / `backend.tf` / `variables.tf` / `data.tf` / `dns.tf` / `worker_route.tf` / `outputs.tf` / `backend.hcl.example`）；`terraform fmt -check` / `init -backend=false` / `validate` 三步无凭据全绿；`plan` / `apply` 待用户本地凭据执行（步骤见 [[getting-started.md|getting-started §6]]）。
  - **provider 5.x 适配要点**（同步落 [[prds/m1-infrastructure.md#7-terraform-细节|PRD §7]] 新增行 + [[tasks/m1/04-terraform-cloudflare.md|tasks/04]] 完成标准）：
    - DNS 资源 `cloudflare_record` → `cloudflare_dns_record`（复数化）
    - worker route 资源 `cloudflare_worker_route` → `cloudflare_workers_route`（复数化），字段 `script_name` → `script`
    - `cloudflare_zone` data source 顶层 `name =` 废弃，必须用 `filter = { name = ..., account = { id = ... }, match = ... }` 块结构（`filter.account.id` 嵌套而非 `account_id`）
    - provider 块不再接受 `account_id`（4.x 已删），账户由 API token 隐式解析；防御性 account 约束下放到 `data.cloudflare_zone.main.filter.account.id`（避免同名 zone 跨账户误匹配）
  - **文档落地**：新建 [[getting-started.md]]（本地起步 9 节——前置依赖 / clone / 各包 dev / VS Code 一键启动 / wrangler deploy / Terraform 起步 / 自验 / FAQ / 下一步）+ 仓库根 `README.md` 同步补"起步"段与仓库结构（指向 `getting-started.md` / `roadmap.md` / `current-state.md`）。两个文件覆盖了 PRD §10 验收与 03 / 04 任务的对外输出。
  - **待人项**：apply 需用户凭据（CF token + AWS S3 凭据），见上方 TODO；apply 完成后 [[tasks/m1/05-github-actions-ci.md|tasks/05]] 解锁。
- 2026-09-04（task 02 完成 + review 通过） — [[tasks/m1/02-shared-envelope-prototype.md|02]] 完成并 review 通过：Zod envelope discriminated union（`HelloEnvelope` / `PingEnvelope` / `EchoEnvelope` + `HelloPayload` / `PingPayload` / `EchoPayload`，payload schema 带 `Schema` 后缀位于 `messages.ts`），5 用例（合法 hello 解析 / `v: 2` 拒绝 / 未知 kind 拒绝 / 未知 role 拒绝 / 缺 `id` 拒绝）全绿。review 修复：(a) 移除 `envelope.ts` 对 payload 类型的冗余再导出，统一通过 barrel `export * from './protocol/messages.js'` 暴露，避免两套入口；(b) 命名契约正式裁定——envelope schema+type 同名（`XxxEnvelope`），payload schema 带 `Schema` 后缀（`XxxPayloadSchema`）、类型不带后缀（`XxxPayload`），M2 扩展 kind 沿用（已落 [[prds/m1-infrastructure.md#2-共享包与协议雏形|PRD §2]]）。**顺带修复**：`vitest.workspace.ts` 的 workspace 入口由相对 glob 改为绝对路径（`${repoRoot}packages/*`、`${repoRoot}worker`）——相对 glob 在 `pnpm --filter <pkg> test` 时按调用方 CWD 解析，包目录内运行 vitest 会找不到兄弟包；用 `import.meta.url` 锁住 repoRoot 后从根或包内调用都能解析正确。
- 2026-09-04（task 01 完成 + review） — [[tasks/m1/01-monorepo-scaffold.md|01]] 完成并 review 通过：脚手架（根 package.json / pnpm-workspace / tsconfig.base / eslint flat / prettier / .nvmrc / .gitignore）+ 四包空壳（shared / bridge / web / worker）+ .vscode 调试四件套（launch.json 三配置 / tasks.json 四后台任务 / settings.json / extensions.json）全部落地；review 修复 7 项（prettierignore / shared 双脚本 / launch.json 字段 / wrangler→4.128+compat 2025-09-01 等）。技术备注：(a) `.prettierignore` 保留 `docs/` 与 `pnpm-lock.yaml` / `pnpm-workspace.yaml` 排除——prettier 3.x 默认格式化 md/yaml，全量格式化 docs 待后续单独做后再放开排除；(b) wrangler 升 4.128 后对 `@cloudflare/workers-types` 有 unmet peer 警告（peer `^5`、现装 v4），当前 dry-run / typecheck 全绿，留待 M2 引入真实 worker 逻辑时再对齐版本。
- 2026-09-04（追加 .vscode 调试四件套） — 用户补充需求：M1 范围内追加本地调试/调试任务配置。新增 [[prds/m1-infrastructure.md#11-本地调试与一键启动vscode|PRD §11 本地调试与一键启动（.vscode）]]（含 `tasks.json` 四任务 / `launch.json` 三配置 / `settings.json` / `extensions.json` 完整定义），目录树补 `.vscode/` 条目，§10 用户清单前插第 0 条（可选 VS Code 一键启动），验收标准新增「### 本地调试（.vscode）」一组；同步收紧 [[tasks/m1/01-monorepo-scaffold.md|任务 01]] 完成标准（worker dev:inspector + .vscode 四件套全部用 `${workspaceFolder}`）。
- 2026-09-04（PRD 落盘 + 任务拆分） — [[prds/m1-infrastructure.md|M1 PRD]] 定稿（基础设施基座）；按 PRD 拆 5 个任务 [[tasks/m1/01-monorepo-scaffold.md|01]] / [[tasks/m1/02-shared-envelope-prototype.md|02]] / [[tasks/m1/03-hello-worker.md|03]] / [[tasks/m1/04-terraform-cloudflare.md|04]] / [[tasks/m1/05-github-actions-ci.md|05]]（全 todo），依赖链 01→02→03→04→05。版本锚定按本地实测修正：`pnpm@9.15.0`→`pnpm@10.32.1`、engines `pnpm >=9 <10`→`>=10 <11`、`pnpm/action-setup` `version: 9`→`10`；Terraform `~> 1.10`→`~> 1.15`、`terraform_version: 1.10.x`→`1.15.x`（PRD §7/§8/§9、根 package.json、CI workflows、4/5 任务清单一致）。`docs/architecture/protocol/README.md` 按 PRD 验收要求保持占位不动。
- 2026-09-04（定稿） — 用户定稿路线图：从早先 M0–M5 六段草案改为 **M1–M4 四步走**（基建 → 通路 → 单 session 闭环 → 多 session 管理）；同步更新 [[roadmap.md]] §5 里程碑表与 §6 待决问题（加归属列）；新建 [[architecture/protocol/README.md|architecture/protocol/]] 作为 RemotePi 隧道协议（envelope / 握手 / token 配对 / 业务消息 / 错误码 / 版本化）的唯一真相源；[[architecture/README.md]] / [[architecture/overview.md]] / [[README.md]] 地图补 protocol 入口；扩展 UI 对话框桥接并入 M3。
- 2026-09-04 — 修正 [[roadmap.md]] / [[architecture/overview.md]] 三处事实性错误：§4.2 线路协议改用物理方向（stdin / stdout）表述并校正事件名与 `extension_ui_request` 方法清单；§4.6 SessionManager 包名更正为 `@earendil-works/pi-coding-agent` 并补 `SessionInfo[]` 字段；§2 拓扑图重画为 web—CF—bridge 三段式（bridge 本地 spawn pi 子进程）。
- 2026-09-03 — 初始化文档：新增 [[roadmap.md]]（路线图 + 架构总览 + pi RPC 协议要点 + M0–M5 里程碑 + 待决问题）；新增 [[architecture/decisions/0001-three-component-topology-with-cf-do.md\|ADR-0001]] ~ [[architecture/decisions/0004-extension-ui-dialog-forwarding.md\|ADR-0004]]（关键设计决策）；[[architecture/overview.md]] 替换占位内容为真实系统总览；[[glossary.md]] 补核心术语；[[README.md]] 地图加上路线图入口。